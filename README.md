### Toy Chipher

This is a python (slow af) implemntation of an spn.
The program takes a file path and a integer key ('-k' option) as input and writes the encrypted file on the path specified with the '-o' option.
